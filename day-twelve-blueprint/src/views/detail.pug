extends layout.pug

block content
    h1=movie.title
    h6=movie.year
    h6=movie.rating
    h6=movie.synopsis
    ul
        each item in movie.genres
            li=item
    a(href=`/${movie.id}/edit`)
        button Edit